import numpy as np
def from_matrix_to_number(F):
  number = ''
  for i in range(F.shape[0]):
    for j in range(F.shape[1]):
      if F[i][j] != 0:
        number += '1' * F[i][j]
        number += '0'
      else:
        number += '0'
  return int(number, base = 2)
